setvar veterinary_target
while hits > 0 and skill 'veterinary' < 120
    target veterinary_target
    dclicktype 'clean bandage%s%'
    waitfortarget
    target veterinary_target
    wait 1000
    if not insysmsg 'not damaged'
        wait 5000
    endif
    hotkey 'Cancel current target'
endwhile
say 'all follow'