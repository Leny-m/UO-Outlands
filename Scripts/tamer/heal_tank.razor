while hits > 0
    overhead 'Healing tank' 0
    if findtype '3617' backpack as bandage
        dclick bandage
        wft 500
        target tank_pet
        wait 6000
        hotkey 'Cancel current target'
    endif
endwhile